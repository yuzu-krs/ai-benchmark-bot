# Test fixtures

Artificial minimal samples used by parser/classification tests. They pin the
contract of each upstream document shape; they are not real captured data.

- `aa-models-free.json` / `aa-models-free-page2.json`: synthetic two-page
  Artificial Analysis free-tier payload (null/absent evaluations, tied scores,
  an id repeated across pages) pinning the models-list contract.
